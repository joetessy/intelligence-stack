# Unlimited-OCR (Universal fork) — pinned to the model's tested environment.
# Installed into ocr/.venv (Python 3.12; the model requires >=3.12,<3.13).
# On macOS/arm64 these are CPU/MPS wheels — no CUDA index needed.
torch==2.10.0
torchvision==0.25.0
transformers==4.57.1
Pillow==12.1.1
matplotlib==3.10.8
einops==0.8.2
addict==2.4.0
easydict==1.13
pymupdf==1.27.2.2
psutil==7.2.2
